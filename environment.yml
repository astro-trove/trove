# Conda environment for TROVE / SAGUARO TOM
# Python 3.11 or 3.12 required (django-autocomplete-light==4.0.0 needs 3.11+; tom-nonlocalizedevents does not support 3.13+)
name: trove
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - -r requirements.txt
